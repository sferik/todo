<ol id="<%= list_name %>">
<% list.each do |todo_item| %>
  <li>
    <span><%= todo_item.name %></span>
    <span><%= todo_item.due_at.strftime("%A, %B %e, %Y") %></span>
    <span><%= link_to 'Destroy', todo_item, method: :delete, data: { confirm: 'Are you sure?' } %></span>
  </li>
<% end %>
</ol>